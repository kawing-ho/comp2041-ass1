#!/usr/bin/python3
# testing some subset3 features
# inline comments testing as well

import sys #import sys

sys.stdout.write("Enter three numbers each on a newline: \n")  #yeah ikr

r = int(sys.stdin.readline()) + int(sys.stdin.readline()) + int(sys.stdin.readline()) #will this fail?

sys.stdout.write("The result is: ") #the ting go skrrraaaa pa pa pa
print(r)  # r r r r r

